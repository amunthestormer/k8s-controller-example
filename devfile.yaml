schemaVersion: 2.2.0
metadata:
  generateName: empty-ubuntu-
components:
  - name: runtime
    container:
      image: gitpod/workspace-full
      args:
        - tail
        - '-f'
        - /dev/null
